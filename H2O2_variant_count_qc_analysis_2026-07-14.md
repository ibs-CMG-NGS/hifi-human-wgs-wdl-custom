# H2O2 WGS 배치 — 변이 수 QC 버그 및 VAF 분포 이질성 분석

**작성일:** 2026-07-14
**대상 데이터:** H2O2 primary cultured mouse astrocyte WGS, 4-well(A/B/C/D) × 3 시퀀싱 배치 = 12 샘플
**파이프라인:** `hifi-human-wgs-wdl-custom` (`workflows/singleton.wdl`, `humanwgs_singleton` v3.1.1), 레퍼런스 GRCm39

---

## 1. 배경

- 실험 설계: 12개 샘플 전부 **하나의 배양·약물처치 배치**에서 준비됨. A01=control, B01/C01/D01=서로 다른 처치(기대 효과가 각각 다름).
- gDNA 추출 → 라이브러리 제작 → WGS 시퀀싱은 **3개의 별도 배치**로 진행:
  - 배치1: `H2O2-A01-ctrl1`, `H2O2-B01`, `H2O2-C01`, `H2O2-D01`
  - 배치2: `H2O2-2-A01`, `H2O2-2-B01`, `H2O2-2-C01`, `H2O2-2-D01`
  - 배치3: `H2O2-3-A01`, `H2O2-3-B01`, `H2O2-3-C01`, `H2O2-3-D01`
- 각 웰(A/B/C/D)의 3개 배치는 **동일 처치 조건의 생물학적 replicate**로 취급 가능.
- 모든 샘플은 `scripts/generate_qc_report.py`로 생성한 QC 리포트에서 처음 검토됨.

---

## 2. 발견 1: QC 리포트 스크립트의 변이 수 카운트 버그

### 2.1 증상

최초 QC 리포트에서 A01(control) 계열이 다른 웰보다 **소변이(small variant) 수가 2~9배 많게** 나타남:

| 샘플 | 리포트 값(버그) |
|---|---|
| H2O2-A01-ctrl1 | 9,215,678 |
| H2O2-2-A01 | 6,361,950 |
| H2O2-3-A01 | 3,222,764 |
| H2O2-B01 | 2,470,144 |
| H2O2-2-B01 | 2,002,021 |
| H2O2-3-B01 | 1,539,107 |

커버리지는 A01이 오히려 더 낮은 경우도 있어(배치1: 25.9×로 전체 최저), "커버리지가 낮은데 변이가 훨씬 많다"는 상식과 반대되는 패턴이 QC 단계에서 포착됨.

### 2.2 근본 원인

`scripts/generate_qc_report.py`의 `count_vcf_variants()` 함수:

```python
# 수정 전 (버그)
result = subprocess.run(
    ['bcftools', 'view', '-H', vcf_file, '|', 'wc', '-l'],
    shell=True, capture_output=True, text=True
)
return int(result.stdout.strip())
```

`shell=True`에 **리스트**를 넘기면, 첫 원소(`'bcftools'`)만 셸 명령으로 실행되고 나머지 원소(`view`, `-H`, `vcf_file`, `|`, `wc`, `-l`)는 무시됨 → `bcftools`가 인자 없이 실행되어 실패 → `int()` 변환 예외 발생 → **항상 `except` 블록의 fallback으로 빠짐**:

```python
# fallback (버그 있음)
with gzip.open(vcf_file, 'rt') as f:
    for line in f:
        if not line.startswith('#'):
            count += 1   # FILTER 상관없이 전부 카운트
```

DeepVariant가 만드는 `phased_small_variant_vcf`에는 **실제 변이(PASS)뿐 아니라 "RefCall"(레퍼런스와 동일, 변이 아님) 레코드도 포함**되어 있음. 이 fallback은 FILTER 값을 구분하지 않고 전부 카운트하므로, RefCall 비율이 높은 샘플일수록 "변이 수"가 크게 부풀려짐.

### 2.3 검증 (H2O2-A01-ctrl1 예시)

```
$ bcftools query -f '%FILTER\n' H2O2-A01-ctrl1...vcf.gz | sort | uniq -c
8704594 RefCall
 511084 PASS
```

`9,215,678 = 8,704,594(RefCall) + 511,084(PASS)` — 버그로 보고된 숫자와 정확히 일치.

구조변이(SV) VCF는 RefCall 같은 non-variant 레코드가 없어 이 버그의 영향을 받지 않음 (기존 SV 카운트는 우연히 정확했음).

### 2.4 수정

```python
def count_vcf_variants(vcf_file):
    """VCF 파일의 PASS variant 개수 카운트 (RefCall 등 non-PASS 레코드 제외)"""
    if not os.path.exists(vcf_file):
        return 0
    import subprocess
    try:
        result = subprocess.run(
            ['bcftools', 'view', '-H', '-f', 'PASS', vcf_file],
            capture_output=True, text=True, check=True,
        )
        return sum(1 for line in result.stdout.splitlines() if line.strip())
    except Exception:
        count = 0
        try:
            import gzip
            with gzip.open(vcf_file, 'rt') as f:
                for line in f:
                    if line.startswith('#'):
                        continue
                    fields = line.split('\t', 7)
                    if len(fields) > 6 and fields[6] == 'PASS':
                        count += 1
        except Exception:
            pass
        return count
```

- `shell=True` 제거, 인자를 정상적인 리스트로 직접 실행 (bcftools 있는 환경에서 정상 동작)
- `-f PASS` 필터 추가 (RefCall 등 제외)
- fallback도 동일하게 FILTER 필드를 확인해 PASS만 카운트하도록 수정 (bcftools 없는 환경에서도 정확)

### 2.5 수정 검증

두 코드 경로(bcftools 있음/없음) 모두, 12개 샘플 전부에서 `bcftools view -H -f PASS | wc -l`로 독립 재계산한 값과 **정확히 일치**함을 확인.

### 2.6 수정 후 실제 변이 수

| 샘플 | 버그(수정 전) | 실제 PASS 변이 수 |
|---|---:|---:|
| H2O2-A01-ctrl1 | 9,215,678 | **511,084** |
| H2O2-B01 | ~2,470,144 | **254,967** |
| H2O2-C01 | ~3,281,171 | **318,799** |
| H2O2-D01 | ~1,618,772 | **201,756** |
| H2O2-2-A01 | 6,361,950 | **402,762** |
| H2O2-2-B01 | 2,002,021 | **229,567** |
| H2O2-2-C01 | 2,465,370 | **313,110** |
| H2O2-2-D01 | 1,263,647 | **197,457** |
| H2O2-3-A01 | 3,222,764 | **364,311** |
| H2O2-3-B01 | 1,539,107 | **218,231** |
| H2O2-3-C01 | 1,755,484 | **255,571** |
| H2O2-3-D01 | 1,114,514 | **191,000** |

버그를 걷어내도 **A01(control)이 B/C/D보다 일관되게 1.7~2배 많음**은 남아있음 → 이건 아티팩트가 아니라 실제 신호로 판단, 아래 3장에서 추가 분석.

---

## 3. 발견 2: VAF 분포로 본 세포 집단 이질성(clonal heterogeneity)

### 3.1 가설

샘플은 primary cultured astrocyte이며, H2O2는 **산화 스트레스를 유발해 세포를 죽이는 처치**임. 따라서:

- **Control(무처치)**: 배양 중 자연 발생한 이질적 세포 클론(또는 오염 세포 유형)이 **그대로 혼재**
- **처치군(H2O2 노출)**: 산화 스트레스에 취약한 클론이 죽고 **생존 클론만 남는 선택압(bottleneck)** 발생 → 더 균질한(clonal) 세포 집단

혼합 클론 gDNA를 단일 이배체로 가정하는 variant caller(DeepVariant)에 넣으면, 이질적 샘플은 **저VAF 잡음성 변이가 늘고, 진짜 동형접합(hom, VAF≈1.0) 위치도 소수 클론에 의해 VAF가 아래로 밀려** 관측된다는 예측.

### 3.2 방법

- 각 샘플의 `phased_small_variant_vcf`에서 PASS 레코드의 VAF(FORMAT 필드) 추출: `bcftools query -f '%FILTER\t[%VAF]\n'`
- 멀티알릴 사이트(VAF가 쉼표로 여러 개 나오는 경우)는 **최댓값**을 대표값으로 사용
  - **주의(분석 중 발견한 자체 버그)**: 최초 시도에서 파이썬 `float()` 파싱이 쉼표 포함 값을 예외로 조용히 버려 12개 샘플 중 6개 샘플에서 개수가 실제보다 적게 집계됨(예: H2O2-3-A01의 경우 339,932 vs 정답 364,311, 차이 24,379 = 멀티알릴 레코드 수와 정확히 일치). 최댓값 처리로 재수정 후 전체 12개 샘플이 §2.6의 검증된 PASS 변이 수와 정확히 일치함을 재확인.
- VAF를 5개 구간으로 분류: `0.00–0.15`(잡음의심) · `0.15–0.35`(저VAF) · `0.35–0.65`(het 피크) · `0.65–0.85`(중간) · `0.85–1.00`(hom 피크)

### 3.3 결과 — A(control) vs B/C/D(처치) 평균 비교 (12샘플)

| VAF 구간 | A 평균 | B/C/D 평균 |
|---|---:|---:|
| 0.00–0.15 (잡음) | 1.6% | 1.8% |
| 0.15–0.35 (저VAF) | **38.6%** | 32.1% |
| 0.35–0.65 (het 피크) | **45.0%** | 37.2% |
| 0.65–0.85 (중간) | 5.7% | 10.6% |
| 0.85–1.00 (hom 피크) | **9.1%** | **18.2%** |

**가장 뚜렷한 신호는 hom 피크(0.85–1.0)** — control이 처치군의 **절반 수준**(9.1% vs 18.2%). 저VAF·het 피크 비중 증가와 함께 나타나, "진짜 hom이어야 할 자리가 이질성 때문에 낮은 VAF 쪽으로 밀려난다"는 가설과 정합적.

### 3.4 결과 — 웰(A/B/C/D)별 배치 간 일관성

| 웰 | batch1 hom% | batch2 hom% | batch3 hom% | batch1 het% | batch2 het% | batch3 het% |
|---|---:|---:|---:|---:|---:|---:|
| A | 6.9 | 9.5 | 10.9 | 49.9 | 45.9 | 39.1 |
| C | 11.9 | 12.9 | 16.9 | 41.6 | 40.5 | 38.5 |
| B | 15.7 | 18.4 | 19.9 | 38.9 | 37.7 | 36.4 |
| D | 21.6 | 23.0 | 23.7 | 34.6 | 33.5 | 33.6 |

**3개 배치 전부에서 hom% 순위가 정확히 동일: A < C < B < D** (이질적 → 균질 순).

각 웰 내에서도 배치1→2→3으로 갈수록 hom%가 단조 증가하는 경향이 관찰됨(배양 조건/계대 수 차이 가능성, 별도 확인 필요).

### 3.5 해석

- 3회 독립 시퀀싱 배치에서 동일한 웰 순위가 재현된 것은 우연이라 보기 어려움 → **웰별 처치 강도에 비례하는 재현 가능한 생물학적(혹은 배양상) 신호**로 판단됨.
- A(control) < C < B < D 순서가 실험에서 기대되는 처치 강도 순서와 일치하는지 확인 필요 (실험 설계상 각 처치의 세포 독성/선택압 세기와 대조).
- 만약 일치한다면, **hom-peak 비율(또는 het/hom 비 등 VAF 분포 요약치)을 "세포 집단 클론성/처치 강도"의 정량적 분자 지표로 활용 가능**할 것으로 보임 — 원래 QC 목적과 무관하게 발견된 부산물이지만 유용성이 있어 보임.

### 3.6 한계 및 확인 필요 사항

- 이 분석은 **상관관계 수준의 증거**이며 인과관계를 증명하지 않음.
- 배양 조건(계대 수, 배양 기간, 배치 간 시점 차이 등)이 실제로 A군과 다르게 적용됐는지 실험 기록 확인 필요.
- 대안 가설(유전 배경 차이, 샘플 오염/교차오염)은 이번 분석으로 완전히 배제되지 않음 — 필요시 추가 QC(예: 샘플 간 유전형 일치도 확인, 성염색체 커버리지 등)로 배제 가능.

### 3.7 통계 검정

배치(replicate)가 웰당 n=3으로 작기 때문에, 소표본에 적합한 비모수/정확 검정을 사용함. 입력값은 §3.4의 hom%(0.85–1.00 구간 비율) 3회 측정치.

**검정 결과**

| 검정 | 통계량 | p-value | 해석 |
|---|---|---:|---|
| Kruskal-Wallis (A/B/C/D 4그룹, n=3씩) | H=9.97 | **0.019** | 4그룹의 hom% 분포가 동일하다는 귀무가설 기각 (α=0.05) |
| Kendall's W (3개 배치 순위 일치도, χ² 근사) | W=1.00 | 0.029 | 완전 순위 일치(W=1.0)가 우연이 아님 |
| Kendall's W 정확확률 (근사 없이 직접 계산) | — | **0.0017** | 소표본에서 근사보다 신뢰도 높음; "3개 독립 배치가 4개 웰 순위를 우연히 완전히 동일하게 매길 확률" = (1/4!)² = 1/576 |
| Mann-Whitney U, 인접 쌍별 (A vs C, C vs B, B vs D) | U=0–1 | 0.10–0.20 | 개별 쌍 비교는 유의하지 않음 |

**해석**

- 4개 그룹을 동시에 비교하는 전역 검정(Kruskal-Wallis, Kendall's W)은 모두 α=0.05를 통과함. 특히 정확확률 계산(p≈0.0017)은 근사에 의존하지 않는 값이라 가장 신뢰할 만함.
- 반면 인접 그룹 간 개별 쌍 비교(Mann-Whitney)는 유의하지 않음(p=0.10–0.20). 이는 효과가 없어서가 아니라, **n=3 vs 3인 비모수 정확검정은 완전히 분리된 경우에도 이론적 최소 p-value가 0.10**이라는 구조적 한계 때문임(4그룹을 한 번에 쓰는 전역 검정은 더 많은 정보를 활용해 검정력이 높음).
- 결론: **"세 배치에서 A<C<B<D 순위가 재현된 것은 우연이 아니다"는 명제는 통계적으로 뒷받침됨.** 다만 "B가 D보다 유의하게 낮다"처럼 개별 그룹 쌍 단위의 주장은 현재 n으로는 통계적으로 확정할 수 없음 — 본 분석에서 웰당 replicate를 늘리면 이 부분의 검정력 확보가 가능할 것으로 예상됨.

### 3.8 교차검증 — Joint calling genotype 불일치율

VAF 분포(단일 샘플 기준)와는 완전히 다른 데이터 소스로 동일 가설을 교차검증하기 위해, joint calling(§5 예정, GLnexus) 결과의 **웰당 3 replicate 간 genotype 일치 여부**를 확인함.

**방법**: `call-glnexus` 출력(split 되기 전 multi-sample joint VCF)에서 사이트별로 3개 샘플의 GT를 추출(`bcftools query -f '[%GT\t]\n'`), phasing(`|`/`/`)과 allele 순서(0/1 vs 1/0)를 정규화한 뒤 3개가 모두 동일한지 확인. 불일치율 = 불일치 사이트 수 / 전체 사이트 수.

**결과** (4그룹 전부, joint calling 완료 후)

| 그룹 | 전체 사이트 | 불일치 사이트 | 불일치율 |
|---|---:|---:|---:|
| A01 | 712,323 | 572,214 | **80.3%** |
| C01 | 460,858 | 343,108 | **74.5%** |
| B01 | 358,575 | 258,398 | **72.1%** |
| D01 | 279,556 | 182,235 | **65.2%** |

순위는 **A > C > B > D** (불일치율 내림차순) — §3.4의 hom%(균질성) 순위 **A < C < B < D**를 정확히 반대로 뒤집은 순서와 완전히 일치함. 전체 사이트 수도 A→D로 갈수록 줄어드는데, 이는 §2.6의 PASS 변이 수 순위(그룹별 변이 수가 A>C>B>D 순으로 많음)와도 일치.

**해석상 주의점**: 이 3개 replicate는 **동일 세포주의 반복 시퀀싱이 아니라, 같은 배치에서 배양된 서로 다른 개별 웰**임. 따라서 불일치율의 절대값(65~80%대)은 세포 이질성 노이즈보다 **웰마다 원래 다른 개별 germline 변이(private variant)**가 주된 기여 요인일 가능성이 높음 — 즉 절대값 자체를 "이질성 지표"로 해석해서는 안 됨.

다만 **완전히 다른 데이터 소스(단일 샘플 VAF 분포 vs joint-called genotype 일치도)에서 동일한 4그룹 순위가 재현**됐다는 것은, §3.3–3.4의 결과가 우연이 아니라는 **강력한 독립적 교차검증 증거**로 볼 수 있음.

---

## 4. 앞으로의 군별 비교 분석 시 고려할 점

3장/§3.7에서 확인한 세포 이질성(clonality) 패턴은 QC상의 흥미로운 부산물이 아니라, **비교 대상 군(A/B/C/D) 자체와 체계적으로 얽혀 있는 진짜 교란변수(confounder)**다. 무작위 잡음이라면 replicate를 늘려서 평균 내면 사라지지만, 이건 군마다 크기가 다른 "체계적 편향"이라 반복수를 늘리는 것만으로는 해결되지 않는다.

### 4.1 WGS 변이 분석에 미치는 영향

- **Heterozygosity / ROH 계열 지표** (`bcftools_roh` 등)는 세포 집단 순도에 따라 왜곡됨 — 이질적 샘플(A)은 진짜 ROH(동형접합 구간)도 낮게 잡힐 수 있음.
- **Joint calling(GLnexus)**은 3개 replicate가 "동일 유전형"이라고 가정함. Control군의 replicate 간 genotype 불일치율이 다른 군보다 높게 나올 가능성이 있음 → **joint calling 결과에서 discordant genotype 비율을 군별로 비교하는 QC를 추가로 권장.**

### 4.2 base-mod(5mC/5hmC/6mA) 분석에 미치는 영향 — 가장 중요한 부분

**h2o2-wgs와 base-mod 분석은 완전히 동일한 원본 gDNA(동일 raw HiFi BAM)를 사용한다** (배치1=r84285_20260326, 배치2=r84285_20260402를 두 파이프라인이 공유). 따라서 이 이질성 문제는 base-mod 분석에도 그대로 적용된다.

- 메틸화 percent는 본질적으로 "그 위치에서 메틸화된 read 비율"로, VAF와 수학적으로 동일한 종류의 측정치임.
- Control이 더 이질적인 세포 집단이라면, 측정된 메틸화 %는 **여러 세포 클론의 평균**이고, 처치군은 선택압으로 더 균질해진 집단의 값을 반영함.
- 그 결과, base-mod 분석에서 관측된 **"rescue 효과"(control vs drug_a delta와 drug_ab rescue delta 간 상관 r=-0.615)** 의 일부(또는 상당 부분)가 실제 유전자 발현/후성유전학적 재프로그래밍이 아니라, **세포 집단 구성 변화(어떤 서브클론이 죽고 살아남았는지)를 반영하는 것일 수 있음.**
- G-test 기반 site-level 통계검정(`stats_test.py`)도 "동일 샘플이면 read-count 노이즈만 존재한다"고 가정하는데, 실제로는 **군마다 진짜 분산 구조(variance)가 다를 수 있어**(control이 원래 더 이질적) 이 가정이 깨질 수 있음.

### 4.3 권장 사항

1. **세포 이질성 지표를 공변량으로 명시적으로 포함**: 샘플별 hom-peak%(또는 het/hom 비율)를 계산해 통계 모델에 covariate로 넣거나, 최소한 주요 비교 결과와 나란히 보고.
2. **독립적인 검증 수단 고려**: single-cell 시퀀싱이나 클론 분리 후 시퀀싱처럼, "세포집단 평균 효과"와 "개별세포 재프로그래밍 효과"를 구분할 수 있는 방법.
3. **base-mod의 기존 rescue 결과 재검토**: 델타가 가장 큰 사이트들이 WGS에서 이질성 패턴이 강하게 나온 샘플에 몰려있는지 교차 확인 — 몰려있다면 그 신호는 세포 선택 효과로 재해석 필요.
4. **Joint calling 품질을 군별로 비교**: control군에서 replicate 간 불일치가 유의하게 많은지 확인.
5. **Replicate 증가만으로는 교란변수가 해소되지 않음**: 이건 무작위 노이즈가 아니라 군에 종속된 체계적 차이라, n을 늘리면 "군 간 차이가 재현 가능하다"는 확신은 커지지만, "그 차이가 처치 효과인지 세포선택 효과인지"를 구분하는 문제는 그대로 남음 — 실험설계(예: 배양 기간을 처치군마다 맞추기) 또는 직교적 검증으로 풀어야 함.

---

## 5. Joint 변이셋 기반 군별 비교 분석 (LOH 후보)

4개 웰의 joint calling(GLnexus, §3.8)이 모두 완료된 후, joint 변이셋을 이용해 실제 **군별 비교 분석**(어느 처치군에 특이적인 변이가 있는지)을 시도함. 단, §4에서 정리한 이질성 confound를 고려해 **각 군 내 3-replicate가 concordant(일치)하게 콜된 사이트만** 사용해 노이즈를 먼저 배제함.

### 5.1 방법

1. 각 군(A/B/C/D)의 joint VCF에서 3-replicate genotype이 concordant한 사이트만 추출 (§3.8과 동일한 정규화 방식)
2. 4개 군 **전부에서 concordant하게 콜된 공통 사이트**의 교집합 산출
3. 교집합 내에서 "3개 군은 동일 genotype, 1개 군만 다름" 패턴을 탐색 → 해당 군에 특이적인 변이 후보로 분류

### 5.2 결과

공통(4군 모두 concordant) 사이트: **49,780개**

| 패턴 | 사이트 수 |
|---|---:|
| 4군 전부 동일 (배경 변이) | 49,466 |
| **A 특이적** | **150** |
| B 특이적 | 5 |
| C 특이적 | 8 |
| **D 특이적** | **65** |
| 기타 (2:2 분할 등) | 86 |

**균등분포 대비 카이제곱 검정**: 관측치 [A=150, B=5, C=8, D=65] vs 기대치(균등, 228/4=57)에 대해 χ²=242.4, **p=2.85×10⁻⁵²** — 군별 편차가 우연이 아님이 압도적으로 뒷받침됨.

### 5.3 패턴의 방향성 — LOH(loss of heterozygosity) 가설

두 특이적 패턴의 방향이 다름:

- **A 특이적(150개)**: A만 이형접합(0/1)이고 B/C/D는 전부 동형접합(1/1) → **A만 heterozygosity를 유지**
- **D 특이적(65개)**: D만 동형접합(1/1)이고 A/B/C는 전부 이형접합(0/1) → **D에서만 추가적으로 homozygosity(LOH) 획득**

즉:
- A(무처치) → B, C 처치에서 일부 위치가 LOH (A_specific 150개가 이 차이를 반영)
- B, C → D(가장 강한/복합 처치로 추정)에서 **한 단계 더** 진행된 추가 LOH (D_specific 65개)

이는 §3.4, §3.8에서 확인한 **균질성/불일치율 그라디언트(A<C<B<D 균질성, A>C>B>D 불일치율)** 와 완전히 같은 방향이며, **세 번째 독립적인 데이터 소스(개별 변이 콜)에서도 동일한 그라디언트가 재현**됨. 산화 스트레스(H2O2)에 의한 DNA 손상이 LOH를 유도하는 것은 이미 알려진 메커니즘이라 생물학적으로도 정합적임.

### 5.4 한계

- 150/65개는 **예비 후보 목록**이며 확정된 변이가 아님. 매핑 아티팩트 가능성을 배제하기 위해 IGV 등으로 일부를 직접 확인 권장.
- LOH 가설은 §3장의 "세포 집단 선택" 가설과 상호 배타적이지 않음 — 오히려 같은 현상(처치 강도에 비례한 클론 균질화)을 서로 다른 각도(집단 구성 vs 개별 유전자좌)에서 보여주는 것으로 해석 가능.
- 후보 변이들이 실제로 어떤 유전자/기능 영역에 위치하는지 주석(annotation)은 아직 수행하지 않음.

---

## 6. 관련 파일

- 수정된 스크립트: `scripts/generate_qc_report.py`
- 재생성된 리포트:
  - `batch_results/H2O2-3_QC_Report.html` (배치3, 4샘플)
  - `batch_results/H2O2_all12_QC_Report.html` (전체 12샘플 통합)
- 이 문서에서 다룬 VAF 분석(§3), 통계 검정(§3.7), joint genotype 불일치율(§3.8), 군별 비교 분석(§5)은 모두 임시 스크래치 디렉토리에서 수행되어 저장소에 별도 스크립트로 남아있지 않음 — 재현이 필요하면 §3.2, §3.7, §3.8, §5.1의 방법을 참고해 재작성 필요.
- Joint calling 결과 위치:
  - `/mnt/hdd1_1tb/h2o2-wgs/joint-H2O2-A01/`, `joint-H2O2-B01/`
  - `/mnt/hdd2_1tb/h2o2-wgs/joint-H2O2-C01/`, `joint-H2O2-D01/`
